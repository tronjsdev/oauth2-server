/*
import { AppConfig } from '../config';

declare namespace Express {
  interface Request {
    // body?: any;
    session?: any;
    user?: any;
    locals?: any;
    //flash: (...props)=>void,
    app: {
      locals: {
        settings: AppConfig;
      };
    };
  }
}
*/
